language: r
cache:
  - packages
  - apt: true


addons:
  apt:
    update: true
    packages:
      - libquantlib0-dev
      - libboost-dev
      - libboost-test-dev

after_success:
  - Rscript -e 'covr::codecov()'
